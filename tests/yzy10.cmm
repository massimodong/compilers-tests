//assign bool as arith
int main() {
    int n, m[2];
    n = read();
    if(((m[0] = (n > 1)) + (m[1] = (n < 4))) == n) {
        write(996);
    }
    write(m[0]);
    write(m[1]);
    write(n);
    return 0;
}
