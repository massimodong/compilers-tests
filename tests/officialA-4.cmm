int compare(int a, int b) {
  if (a == b)
    return 0;
  if (a < b)
    return -1;
  else
    return 1;
}

int compare(float x, float y) {
  return -2;
}

int main(){
  compare(1,2);
  compare(1.2,3.4);
  return 0;
}
