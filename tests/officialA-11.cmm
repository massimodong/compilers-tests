int gcd(int x, int y){
  if(x == 0) return y;
  return gcd(y - x, x);
}

int main() {
  int gdd, i, N, sum;
  gdd = 14;
  N = 12;
  i = 0;
  sum = 0;
  while(i < N) {
    i = i+ 1;
    sum = sum + gcd(gdd, i);
  }
  return sum + gdd(gdd, N * 2);
}
