struct Burger{
  int meat;
  float saurce;
  int bread;
}king;

int addMeat(int addedAmount);
int addMeat(int addedAmount, struct Burger addedBurger);

int addMeat(int addedAmount){
  king.meat = king.meat + addedAmount;
  return 0;
}

float addSaurce(float addedSaurce);

int addSaurce(float addedSaurce) {
  king.saurce = addedSaurce;
  return 0;
}

int addBread(int breads, struct Burger bb);
